module.exports = {
    content: ["./src/**/*.{html,js}"],
    theme: {
        extend: {
            fontFamily: {
                display: 'Raleway', // Adds a new `font-display` class
            }
        },
    },
    plugins: [],
}
